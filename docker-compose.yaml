version: '3.8'

services:
  clickhouse:
    image: clickhouse/clickhouse-server
    container_name: clickhouse-forkTravel
    restart: always
    ports:
      - "8100:8123"
      - "9000:9000"
    volumes:
      - ./datagrip/dump.sql:/docker-entrypoint-initdb.d/dump.sql